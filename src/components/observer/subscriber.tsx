import React from 'react';
import { Observer } from '../../utils/observer';

function Subscriber(props: { observer: Observer}) {
  return (
    <>
      <h4>Subscriber:</h4>
    </>
  );
}

export default Subscriber;
